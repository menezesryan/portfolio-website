//contains the theme colors for elements and backgrounds

import { createTheme } from "@mui/material"

export const theme = createTheme({
    typography: {
        h1: {
            fontSize: 10
        }
    }
})